return {
    array   = require "array",
    async   = require "async",
    event   = require "event",
    guard   = require "guard",
    meta    = require "meta",
    object  = require "object",
    pattern = require "pattern",
    seq     = require "seq",
    spec    = require "spec",
    struct  = require "struct"
}